# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-custom-application"
BBFILE_PATTERN_meta-custom-application = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-custom-application = "6"

LAYERDEPENDS_meta-custom-application = "core"
LAYERSERIES_COMPAT_meta-custom-application = "scarthgap"
